# Local development overrides
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

platform:
  s3:
    enabled: true
    endpoint: http://localhost:4566

logging:
  level:
    com.jobmonitor: DEBUG
    org.hibernate.SQL: DEBUG
