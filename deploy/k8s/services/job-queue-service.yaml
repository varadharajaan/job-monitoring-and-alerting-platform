# ═══════════════════════════════════════════════════════════════════
#  Job Queue Service — Deployment + Service
# ═══════════════════════════════════════════════════════════════════
apiVersion: apps/v1
kind: Deployment
metadata:
  name: job-queue-service
  namespace: jobmonitor
  labels:
    app: job-queue-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: job-queue-service
  template:
    metadata:
      labels:
        app: job-queue-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "8085"
    spec:
      containers:
        - name: job-queue-service
          image: jobmonitor/job-queue-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: jobmonitor-config
            - secretRef:
                name: jobmonitor-secrets
          env:
            - name: JOB_QUEUE_PORT
              value: "8085"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 60
            periodSeconds: 30
          resources:
            requests:
              cpu: 200m
              memory: 384Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: job-queue-service
  namespace: jobmonitor
spec:
  selector:
    app: job-queue-service
  ports:
    - port: 8085
      targetPort: 8085
  type: ClusterIP
